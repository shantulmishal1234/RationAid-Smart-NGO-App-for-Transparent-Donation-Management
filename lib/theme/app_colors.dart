import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primaryBlue = Color(0xFF5CB9DD);
  static const accentGreen = Color(0xFF6DD1A1);

  // Roles
  static const donorGreen = Color(0xFF4CAF50);
  static const purchaserOrange = Color(0xFFFF9800);
  static const volunteerBlue = Color(0xFF2196F3);
  static const adminRed = Color(0xFFF44336);

  // Neutrals
  static const background = Color(0xFFF5F5F5);
  static const cardBackground = Colors.white;
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
}
